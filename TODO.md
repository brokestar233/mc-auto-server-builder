# TODO（未实现与部分实现）

> 判定口径：包含**未实现**与**部分实现**两类。

## 1. 输入解析与优先策略

- [ ] 自动优先检测并下载官方或社区 `Server Pack`，命中后直接走服务端包流程，跳过清理与调试循环
  - 现状：未看到对 CurseForge `Server Files` 或 Modrinth `Server Pack` 的专项探测与短路逻辑，当前主流程仍以客户端包展开与清理为核心

## 2. 服务器核心安装

- [ ] Java 版本策略中的最新 LTS 动态选择未完成
  - 现状：固定优先到 Java 21，未做随生态变化的 LTS 自动前移策略

## 3. 客户端文件处理与清理

- [ ] `resourcepacks` 目录的服务器必要子集提取未实现
  - 现状：整体作为客户端内容排除，没有做服务端相关资源包子集判定

- [ ] 客户端专属内容清理规则的双存储后端未实现
  - 现状：规则库以 SQLite 为主，草案提到的 SQLite/JSON 双形态未完整落地

## 4. 启动探测与调试循环

- [ ] 每次尝试前备份 `mods` 与启动脚本的双备份未实现
  - 现状：已有 `mods` 备份，未看到启动脚本按 `attempt_N` 独立备份

- [ ] 启动期进程资源占用的实时监控与记录未实现
  - 现状：已有日志尾部与端口探测，但缺少 CPU/内存曲线或快照记录

## 5. AI 与模型能力

- [ ] 本地小模型多后端适配目标仅部分实现
  - 现状：已支持 `ollama` 与 openai-compatible HTTP；草案中的 `llama.cpp`、`MLX`、`Transformers + bitsandbytes` 直连尚未实现

- [ ] AI 输入模板与草案模板完全对齐未实现
  - 现状：已有结构化 prompt 与 JSON 约束，但字段与格式已演进，不是草案模板的严格镜像

## 6. 产物与报告增强

- [ ] 报告中删除 mod 的来源分层统计仍需增强
  - 现状：有删除列表与操作记录，但未严格按 内置规则/正则规则/AI 建议 三类结构化汇总

## 7. 增强功能

- [ ] 黑名单与正则数据库在线更新能力未实现

- [ ] 简单 GUI 界面未实现
